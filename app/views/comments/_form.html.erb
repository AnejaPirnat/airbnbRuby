<%= form_with(model: [post, category, adress, comment]) do |form| %>
  <div>
   <%= form.label :body, style: "display: block"%>
   <%= form.text_area :body, required: true%>
  </div>

    <%= form.submit %>
   </div>
<% end %>